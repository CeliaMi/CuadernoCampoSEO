{% extends 'base.html.twig' %}

{% block title %}Alerta index{% endblock %}

{% block body %}
    <h1>Alerta index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Ubicacion</th>
                <th>Foto</th>
                <th>Tipo de Amenaza</th>
                <th>Superfice afectada</th>

                <th>OPCIONES</th>
            </tr>
        </thead>
        <tbody>
        {% for alertum in alertas %}
            <tr>
                {# <td>{{ alertum.id }}</td> #}
                <td>{{ alertum.ubicacion }}</td>
                <td>{{ alertum.foto }}</td>
                <td>{{ alertum.nombreTipoDeAmenaza }}</td>
                <td>{{ alertum.superficieAfectada }}</td>

                {# <td>{{ alertum.descripcion }}</td>
                <td>{{ alertum.nombreContacto }}</td>
                <td>{{ alertum.emailContacto }}</td>
                <td>{{ alertum.telefonoContacto }}</td> #}
                <td>
                <button><a href="{{ path('app_alerta_show', {'id': alertum.id}) }}">show</a></button>
                <button><a href="{{ path('app_alerta_edit', {'id': alertum.id}) }}">edit</a></button>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <button><a href="{{ path('app_logout') }}">Salir</a> </button>
{% endblock %}